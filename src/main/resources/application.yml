spring:
  datasource:
    url: jdbc:mysql://43.143.64.121:3306/kfpt?serverTimezone=UTC&useUnicode=true&characterEncoding=UTF-8&allowMultiQueries=true
    username: root
    password: 1qaz@WSX
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis-plus:
  # 配置Mapper.xml映射文件
  mapper-locations: classpath*:/mapper/*Mapper.xml
server:
  port: 8019
logging:
  file:
    name: logs/info.log